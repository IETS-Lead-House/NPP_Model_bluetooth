<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                                   android:layout_width="match_parent"
                                                   android:layout_height="48sp" android:id="@+id/label">

    <TextView
            android:text="@string/error_name"
            android:layout_width="0dp"
            android:layout_height="24sp" android:id="@+id/bluetooth_device_item__name"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintVertical_bias="0.0" android:textSize="20sp"/>
    <TextView
            android:text="@string/error_address"
            android:layout_width="0dp"
            android:layout_height="20sp" android:id="@+id/bluetooth_device_item__address"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/bluetooth_device_item__name" app:layout_constraintVertical_bias="0.0"
            android:layout_marginTop="2dp" android:textSize="16sp"/>
</androidx.constraintlayout.widget.ConstraintLayout>